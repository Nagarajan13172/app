# Base URL of the NestJS backend (global prefix /api). The backend runs on
# port 3000, so this Next.js app runs on 3001 (see package.json dev script).
# NEXT_PUBLIC_ vars are inlined into the client bundle at build time.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api
